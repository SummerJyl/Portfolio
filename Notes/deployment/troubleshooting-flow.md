The Lesson: The "Three-Step Refresh" for cached web apps.

Rebuild: Ensure npm run build completed without errors.

Push: Confirm the ./deploy.sh script actually finished the git push to the gh-pages branch.

Hard Refresh: Use Cmd + Shift + R to force the browser to drop the old cached bundle and fetch the new one.